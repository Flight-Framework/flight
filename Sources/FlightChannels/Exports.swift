/// Re-exported so `import FlightChannels` brings the wire vocabulary —
/// `Envelope`, `JSONValue`, `ReservedEvent`, the error reasons — without a
/// second import, the same way FlightWeb re-exports the HTTP types.
@_exported import FlightChannelsProtocol
