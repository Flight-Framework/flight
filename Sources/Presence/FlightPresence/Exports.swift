/// Re-exported so `import FlightPresence` brings the wire vocabulary —
/// `PresenceEntry`, `PresenceMeta`, `PresenceEvent`, `PresenceSync` — the
/// same way FlightChannels re-exports its protocol target.
@_exported import FlightPresenceProtocol
