/// Re-exported so `import FlightChannelsClient` brings the wire vocabulary —
/// `Envelope`, `JSONValue`, `ReservedEvent`, error reasons — without a
/// second import.
@_exported import FlightChannelsProtocol
